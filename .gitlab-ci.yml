stages:
  - test

run all tests:
  image: node:buster-slim
  before_script:
    - npm install --also=dev
  script:
    - npm test
  cache:
    paths:
      - node_modules
  only:
    - main
    - merge_requests
